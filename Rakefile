require_relative "env"

desc "Setup - Install prerequisites"
task :setup do

end

desc "Prepare Dataset - prepare your images to be trained on by StyleGAN3"
task :prepare_dataset do

end

desc "Train - train model with your image dataset"
task :train do

end

desc "Stats"
task :stats do
  StyleGAN3Manager.utils.f(:stats).()
end

task default: :train
